{
  "scenarios": [
    {
      "name": "vectors/keys-z32-sign",
      "description": "Go verifies endpoint IDs, z-base-32 encodings, signatures, and signed packets generated by Rust, and a pass proves byte-compatible key and signature representations.",
      "tier": "stable",
      "counterpart": "Rust test driver",
      "expected": {"1.0": "pass"}
    },
    {
      "name": "vectors/postcard-varints",
      "description": "Go decodes postcard integer encodings generated by Rust, and a pass proves byte-compatible varint serialization at the tested boundaries.",
      "tier": "stable",
      "counterpart": "Rust test driver",
      "expected": {"1.0": "pass"}
    },
    {
      "name": "vectors/postcard-8bit",
      "description": "Go encodes u8 and i8 values and compares them to the Rust driver's output at 0, 1, 127, 128, 200, 255 and -128, -2, -1, 0, 127, and a pass proves that both write a single raw byte rather than a varint above the 128 boundary.",
      "tier": "stable",
      "counterpart": "Rust test driver",
      "expected": {"1.0": "pass"}
    },
    {
      "name": "vectors/endpoint-ticket-roundtrip",
      "description": "Go decodes and re-encodes endpoint tickets generated by Rust, and a pass proves a byte-identical ticket round trip.",
      "tier": "stable",
      "counterpart": "Rust test driver",
      "expected": {"1.0": "pass"}
    },
    {
      "name": "vectors/pkarr-txt",
      "description": "Go verifies and parses a Rust-generated signed pkarr TXT packet, and a pass proves compatible packet signatures and TXT payload encoding.",
      "tier": "stable",
      "counterpart": "Rust test driver",
      "expected": {"1.0": "pass"}
    },
    {
      "name": "vectors/gossip-frame",
      "description": "Go and the Rust test driver exchange framed gossip broadcasts in both directions, and a pass proves compatible topic, sender, and payload encoding.",
      "tier": "stable",
      "counterpart": "Rust test driver",
      "expected": {"1.0": "pass"}
    },
    {
      "name": "vectors/custom-addr-ticket-rust-to-go",
      "description": "Go decodes CustomAddr endpoint tickets generated by Rust at lengths 0, 1, 29, 30, 31, and 255, and a pass proves Rust-to-Go compatibility across the inline-storage boundary.",
      "tier": "experimental",
      "counterpart": "Rust test driver",
      "expected": {"1.0": "fail", "1.1-pre": "pass"}
    },
    {
      "name": "vectors/custom-addr-ticket-go-to-rust",
      "description": "Rust decodes CustomAddr endpoint tickets generated by Go at lengths 0, 1, 29, 30, 31, and 255, and a pass proves Go-to-Rust compatibility across the inline-storage boundary.",
      "tier": "experimental",
      "counterpart": "Rust test driver",
      "expected": {"1.0": "fail", "1.1-pre": "pass"}
    },
    {
      "name": "vectors/postcard-varint-strictness",
      "description": "Go and the Rust driver decode the same canonical and non-canonically padded varint byte strings, and the recorded verdict is that they diverge: go-iroh rejects the padded encodings that postcard 1.1.3 accepts.",
      "tier": "stable",
      "counterpart": "Rust test driver",
      "expected": {"1.0": "fail"}
    }
  ]
}
