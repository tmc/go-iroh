[package]
name = "go-iroh-no-sni-client"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
iroh = { version = "=1.2.0", default-features = false, features = ["tls-aws-lc-rs"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
